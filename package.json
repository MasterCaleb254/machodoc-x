{
  "name": "machodoc-x-monorepo",
  "version": "1.0.0",
  "private": true,
  "workspaces": ["mobile", "backend", "shared", "ml-models"],
  "scripts": {
    "dev:mobile": "cd mobile && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "test": "lerna run test",
    "build": "lerna run build",
    "lint": "lerna run lint",
    "typecheck": "lerna run typecheck"
  },
  "devDependencies": {
    "lerna": "^6.6.2",
    "typescript": "^5.0.0",
    "@typescript-eslint/eslint-plugin": "^6.0.0",
    "eslint": "^8.45.0",
    "prettier": "^3.0.0",
    "husky": "^8.0.0"
  }
}

